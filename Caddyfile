# --- vital-pulse ---------------------------------------------------------------
# FrankenPHP serves the entire application (API + static dashboard) on port 80.
# The host-level Caddy just terminates TLS and forwards.
#_______________________________________________________________________________

vitals.devgnome.com {
	reverse_proxy vital-pulse
}
